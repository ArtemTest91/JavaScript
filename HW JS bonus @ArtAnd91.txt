1. Создать переменную a
let a;

2. Создать переменную b
let b;

4. Присвоить a - число 50, b - число 20 
a = 50;
b= 20;

3. Создать переменную c и присвоить ей сумму a и b, деленную на произведение a и b
let c = (a+b) / (a*b);

4. Вывести c
console.log (c);

5. Вывести с, a и b в одну строку через запятую
console.log (c + ', ', b + ', ', a);

6. Вывести сумму a + b + с , разницу между a и b и остаток от деления a на b в одну строку, не используя других переменных в виде:
"Сумма a, b и с == ... , Дельта a и b == ... , Остаток от деления a на b == ... ":

console.log (('Остаток от деления a на b == ' + (a%b)) + ", " + ('Сумма a, b и c == ' + (a+b+c)) + ", " + ('Дельта a и b == ' + (a-b)));


1* Создать функцию, которая будет выводить утроенное значение остатка от деления произведения переменных "a" и "b" на их сумму:

function VeryHard (a,b) {
    let l = ((a*b) % (a+b))*3;
    console.log (l);
}
VeryHard (a,b)

2* Создать функцию, которая будет получать на вход число и на выходе будет писать - делится ли оно на 5 или делится ли оно на 11
Если число не делится ни на 5, ни на 11 - вывести " ... - скучное число ":

let j=55;
function SoHard (j) {
    if (j%5==0) {
        console.log ('Делится на 5');
    } else if (j%11==0) {
        console.log ('Делится на 11');
    } else {
        console.log ('Скучное число');
    }
}
SoHard (j)

3*** Подумать и предусмотреть, какие случаи могут баговать предыдущую функцию и добавить проверки на невалидные вводы в таких случаях вывести в чем ошибка ввода:
let w=55
function W (w) {
    if (typeof w != "number") {
        console.log ('Необходимо использовать только числа и цифры');
    } else if (w==0) {
        console.log ('Значение "ноль" недопустимо');
    } else if (w==Infinity) {
        console.log ('Значение "бесконечность" недопустимо');
    } else if (w==55) {
        console.log ('Верное число');
    } else if (w%5==0) {
        console.log ('Делится на 5');
    } else if (w%11==0) {
        console.log ('Делится на 11');
    } else {
        console.log ('Скучное число');
    }
}
W (w)